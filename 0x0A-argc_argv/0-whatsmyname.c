#include "main.h"
#include <stdio.h>
/**
 * main - countains main function
 * @argc: for argument count
 * @argv: for argument vector
 * Return: always return 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
