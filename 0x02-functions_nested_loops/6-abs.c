#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @s: the integer to compute the absolute value of
 *
 * Return: the absolute value of s
 */
int _abs(int s)
{
	if (s < 0)
	{
	return (-s);
	}
	else
	{
	return (s);
	}
}
